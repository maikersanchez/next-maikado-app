# URL del backend para comunicación interna entre contenedores
BACKEND_INTERNAL_URL=http://backend:8000

# URL del backend para comunicación desde el navegador del cliente
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000
